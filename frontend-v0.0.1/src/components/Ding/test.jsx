import React, { useState } from "react";
import axios from "axios";
const Test = () => {
    // const[val , setVal] = useState(1);
    // React.useEffect(()=> {
    //     axios.get("http://localhost:5015/api/inandout").then((resp)=> {setVal(resp.data)})
    // } , [])

    // Uint32Array(16 , 32 , 32 , 16 , 8);
    // return (<h1>{val}</h1>);
}
export default Test;